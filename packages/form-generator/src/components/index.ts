import { Widgets } from '../Widgets';

export const Components: { [key in Widgets]: string } = {
	EntityPicker: 'EntityPicker',
	Stat: 'Stat',
	MultiSelect: 'MultiSelect',
	RichText: 'RichText',
	Select: 'Select',
}
