export interface LegacyCoreUiTheme {
	colors: {
		inputBackground: string;
		text: string;
		textDisabled: string;
		primary: string;
		primaryHover: string;
		primaryActive: string;
		background: string;
	};
}
